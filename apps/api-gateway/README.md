# api-gateway

Ponto único de entrada da API: autenticação JWT (Firebase), rate limiting
e roteamento para os microserviços internos (request, delivery, user,
notification, ai-assistant).

Ainda não implementado — entra junto com a Fase 2, quando os primeiros
microserviços já expõem endpoints reais para rotear.
